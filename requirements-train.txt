# Extras for re-running the LOCO experiments (torch + first-order MAML).
# NOTE: learn2learn 0.2.0 does not build on Python >= 3.11 (its generated C
# uses longintrepr.h, removed in 3.11); use a Python 3.9 environment, which
# is what all recorded results were produced with.
-r requirements.txt
torch
learn2learn==0.2.0
requests
